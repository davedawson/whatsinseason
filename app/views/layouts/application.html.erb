<%= render 'partials/head' %>
<body id="<%= body_id(yield(:body_id)) %>" class="<%= body_class(yield(:body_class)) %>">

  <%= render 'partials/header' %>
  <%= render 'partials/errors' %>
<div class="content">
  <%= yield %>
</div>

</body>
</html>
